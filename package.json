{
  "name": "attendance-system-v2",
  "version": "2.1.0",
  "description": "Hệ thống chấm công GPS & QR Code",
  "private": true,
  "dependencies": {
    "firebase": "^10.7.0",
    "html5-qrcode": "^2.3.8",
    "lucide-react": "^0.294.0",
    "qrcode": "^1.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-tools": "^13.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "firebase:setup": "node scripts/setup-firebase.js",
    "firebase:init": "node scripts/init-firestore.js",
    "firebase:deploy": "firebase deploy --only firestore:rules,storage:rules,firestore:indexes",
    "firebase:rules": "firebase deploy --only firestore:rules,storage:rules"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
